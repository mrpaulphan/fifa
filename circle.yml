machine:
  node:
    version: 5.10.1
  php:
    version: 5.4.21
dependencies:
  override:
    - npm install --global gulp-cli
    - npm install
    - composer install
    - composer global require "laravel/installer"
  cache_directories:
    - "node_modules"
    - "vendor"
test:
  override:
